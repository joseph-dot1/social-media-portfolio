# Eshenake Joseph — Portfolio

Social media growth specialist portfolio. Next.js (App Router) + Tailwind + Framer Motion + React Three Fiber.

- `npm install && npm run dev` — local dev
- `npm run build && npm start` — production
- All copy and numbers live in `content/` — no strings in components
- Site plan: `docs/SITE_PLAN.md` · Content data: `docs/CONTENT_BRIEF.md`
